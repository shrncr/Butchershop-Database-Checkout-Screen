'''
Only kept this here to refer back to table names. 
So I didnt have to go between datagrip and vscode.
'''

 
# cursor.execute("CREATE DATABASE butchershop")
# cursor.execute("CREATE TABLE Payment (purchaseID INT, lastFourDigits VARCHAR(4), lastNameOnCard VARCHAR(255), total DECIMAL)")
#cursor.execute("CREATE TABLE cart ( transactionID VARCHAR(255), typeOfMeat VARCHAR(30), weight DECIMAL)")

# addMeat = "INSERT INTO Meat (meatID, typeOfMeat, pricePerPound, lbsRemaining) values (%d,%s,%d,%d)"
# meats = [
#     (1, "Chicken", 10.99, 430),
#     (2, "Pork", 9.40, 60),
#     (3, "Salmon", 15.93, 90),
#     (4, "Beef", 8.99, 100)
# ]
